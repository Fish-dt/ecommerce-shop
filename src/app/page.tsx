export const metadata = {
  title: 'Products',
  description:
    'Browse our wide selection of quality products. Find the perfect item for your needs with fast delivery and excellent customer service.',
};

import { HomeClient } from '@/components/Home/HomeClient';

export default function HomePage() {
  return <HomeClient />;
}
